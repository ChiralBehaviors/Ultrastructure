<?xml version="1.0" encoding="UTF-8"?>

<databaseChangeLog xmlns="http://www.liquibase.org/xml/ns/dbchangelog" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
    http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-3.1.xsd"
>

    <changeSet id="2.0.0" author="hhildebrand">
        <createView viewName="agency_location" schemaName="readable" replaceIfExists="true"><![CDATA[
			 SELECT w.name AS workspace, 
               bl.id, 
               res.name AS agency, 
               rel.name AS relationship, 
               l.name AS location, 
               bl.notes, 
               up.name AS updated_by, 
               bl.version
               
			   FROM agency_location bl
			   JOIN agency res ON bl.agency = res.id 
			   JOIN relationship rel ON bl.relationship = rel.id
			   JOIN location l ON bl.location = l.id
			   JOIN agency up ON bl.updated_by = up.id
             LEFT JOIN ruleform.workspace_authorization wa ON bl.workspace = wa.id
             LEFT JOIN ruleform.product w ON wa.defining_product = w.id
            ]]>
        </createView>
        <rollback>
            <dropView viewName="readable.agency_location" />
        </rollback>
    </changeSet>
</databaseChangeLog>
