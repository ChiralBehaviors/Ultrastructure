<?xml version="1.0" encoding="UTF-8"?>

<databaseChangeLog xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
    http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-3.1.xsd">

    <changeSet id="1" author="hhildebrand">
        <sql><![CDATA[
            CREATE OR REPLACE VIEW readable.location AS 
			 SELECT l.id, l.name, l.description, l.notes, r.name AS updated_by, l.version
			   FROM location l
			   JOIN agency r ON l.updated_by = r.id;
			
			ALTER TABLE readable.location
			  OWNER TO core;
            ]]>
        </sql>
        <rollback>
            <dropView viewName="readable.location" />
        </rollback>
    </changeSet>
</databaseChangeLog>
