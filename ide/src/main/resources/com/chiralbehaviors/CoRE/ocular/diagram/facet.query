query facet($id: String!) {
  facet(id: $id) {
    id
    name
    classifier {
      id
      name
    }
    classification {
      id
      name
    }
    attributes {
      id
      authorizedAttribute {
        id
        name
      }
    }
    children {
      id
      name
    }
  }
}
