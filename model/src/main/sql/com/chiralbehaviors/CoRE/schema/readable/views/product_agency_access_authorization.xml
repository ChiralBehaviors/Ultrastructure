<?xml version="1.0" encoding="UTF-8"?>

<databaseChangeLog xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
    http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-3.1.xsd">

    <changeSet id="1" author="hhildebrand">
        <sql><![CDATA[
            CREATE OR REPLACE VIEW readable.product_agency_access_authorization AS 
             SELECT a.id, p.name AS product, ptr.name AS product_relationship, r.name AS relationship,
                    rsc.name AS agency, ctr.name AS agency_relationship
               FROM access_authorization a
               JOIN product p ON a.product1 = p.id
               JOIN relationship ptr ON a.parent_transitive_relationship = ptr.id
               JOIN relationship r ON a.relationship = r.id
               JOIN agency rsc ON a.agency2 = rsc.id
               JOIN relationship ctr ON a.child_transitive_relationship = ctr.id
               WHERE a.authorization_type = '2®';
            
            ALTER TABLE readable.product_agency_access_authorization
              OWNER TO core;
            ]]>
        </sql>
        <rollback>
            <dropView viewName="product_agency_access_authorization" />
        </rollback>
    </changeSet>
</databaseChangeLog>
