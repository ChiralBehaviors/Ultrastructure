dbaServer: ${testing.db.server}
dbaPort: ${testing.db.port}
dbaUsername: ${testing.db.password}
dbaPassword: ${testing.db.login}

coreServer: ${db.server}
corePort: ${db.port}
coreUsername: ${db.login}
corePassword: ${db.password}

dropDatabase: true
