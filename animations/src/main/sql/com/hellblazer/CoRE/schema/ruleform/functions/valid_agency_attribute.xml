<?xml version="1.0" encoding="UTF-8"?>

<databaseChangeLog xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
    http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-3.0.xsd">
    
    <changeSet id="1" author="hhildebrand">
        <sql splitStatements="false"><![CDATA[
            CREATE OR REPLACE FUNCTION ruleform.valid_agency_attribute()
              RETURNS trigger AS
            $BODY$
            DECLARE
                _type TEXT;
            BEGIN
                IF ( (TG_TABLE_SCHEMA != 'ruleform') AND (TG_TABLE_NAME != 'agency_attribute') ) THEN
                    RAISE EXCEPTION 'Trigger "%" is improperly configured; it must only be attached to the ruleform.agency_attribute table', TG_NAME;
                END IF;
            
                IF ( (TG_WHEN != 'AFTER') OR (TG_OP NOT IN ('INSERT','UPDATE') )) THEN
                    RAISE EXCEPTION 'Trigger "%" on table "%" is improperly configured; it must only run after INSERT or UPDATE operations!', TG_NAME, TG_TABLE_SCHEMA || '.' || TG_TABLE_NAME;
                END IF;
            
                SELECT ruleform.get_attribute_type('agency', NEW.attribute) INTO STRICT _type;
                IF (_type = 'text') THEN
                    IF NEW.text_value IS NULL THEN
                        RAISE EXCEPTION 'Must supply a text value for a text-valued Attribute!';
                    END IF;
                ELSEIF (_type = 'numeric') THEN
                    IF NEW.numeric_value IS NULL THEN
                        RAISE EXCEPTION 'Must supply a numeric value for a numeric-valued Attribute!';
                    END IF;
                ELSEIF (_type = 'boolean') THEN
                    IF NEW.boolean_value IS NULL THEN
                        RAISE EXCEPTION 'Must supply a boolean value for a boolean-valued Attribute!';
                    END IF;
                ELSEIF (_type = 'agency') THEN
                    IF NEW.agency_value IS NULL THEN
                        RAISE EXCEPTION 'Must supply a agency value for a agency-valued Attribute!';
                    END IF;
                ELSEIF (_type = 'product') THEN
                    IF NEW.product_value IS NULL THEN
                        RAISE EXCEPTION 'Must supply a Product value for a Product-valued Attribute!';
                    END IF;
                ELSEIF (_type = 'event') THEN
                    IF NEW.event_value IS NULL THEN
                        RAISE EXCEPTION 'Must supply an Event value for a Event-valued Attribute!';
                    END IF;
                ELSEIF (_type = 'attribute') THEN
                    IF NEW.attribute_value IS NULL THEN
                        RAISE EXCEPTION 'Must supply an Attribute value for a Attribute-valued Attribute!';
                    END IF;
                ELSE
                    RAISE EXCEPTION '% does not know how to handle an attribute of type %', TG_TABLE_SCHEMA || '.' || TG_TABLE_NAME, _type;
                END IF;
            
                RETURN NEW;
            
            END;
            $BODY$
              LANGUAGE plpgsql VOLATILE
              COST 100;
            ALTER FUNCTION ruleform.valid_agency_attribute()
              OWNER TO core;
            ]]>
        </sql>
        <rollback> DROP FUNCTION ruleform.valid_agency_attribute(); </rollback>
    </changeSet>
</databaseChangeLog>
