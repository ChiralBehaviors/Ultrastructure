<?xml version="1.0" encoding="UTF-8"?>

<databaseChangeLog xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
    http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-3.0.xsd">
    
    <changeSet id="1" author="hhildebrand">
        <sql><![CDATA[
            CREATE TABLE ruleform.product_network_deduction
            (
              id bigserial NOT NULL,
              deduction bigint NOT NULL,
              premise1 bigint NOT NULL,
              premise2 bigint NOT NULL,
              updated_by bigint NOT NULL,
              update_date timestamp without time zone NOT NULL DEFAULT ('now'::text)::timestamp(6) with time zone,
              CONSTRAINT product_network_deduction_path_pkey PRIMARY KEY (id ),
              CONSTRAINT product_network_deduction_path_hypothesis_fkey FOREIGN KEY (deduction)
                  REFERENCES ruleform.product_network (id) MATCH SIMPLE
                  ON UPDATE CASCADE ON DELETE NO ACTION DEFERRABLE INITIALLY IMMEDIATE,
              CONSTRAINT product_network_deduction_path_premise1_fkey FOREIGN KEY (premise1)
                  REFERENCES ruleform.product_network (id) MATCH SIMPLE
                  ON UPDATE CASCADE ON DELETE NO ACTION DEFERRABLE INITIALLY IMMEDIATE,
              CONSTRAINT product_network_deduction_path_premise2_fkey FOREIGN KEY (premise2)
                  REFERENCES ruleform.product_network (id) MATCH SIMPLE
                  ON UPDATE CASCADE ON DELETE NO ACTION DEFERRABLE INITIALLY IMMEDIATE,
              CONSTRAINT product_network_deduction_path_updated_by_fkey FOREIGN KEY (updated_by)
                  REFERENCES ruleform.agency (id) MATCH SIMPLE
                  ON UPDATE CASCADE ON DELETE NO ACTION DEFERRABLE INITIALLY IMMEDIATE,
              CONSTRAINT product_network_deduction_path_u UNIQUE (deduction , premise1 , premise2 )
            )
            WITH (
              OIDS=FALSE
            );
            ]]>
        </sql>
        <rollback>
            <dropTable tableName="ruleform.product_network_deduction"/>
        </rollback>
    </changeSet>
</databaseChangeLog>
